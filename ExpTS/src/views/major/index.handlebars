<section>
   
  <table>
    <caption>
        <div class="caption-row">
            <span class="caption-title">Lista de Majors</span>
            <a href="/major/create" class="btn-add-major" title="Adicionar novo curso">
            <i class="fas fa-plus"></i>
            </a>
        </div>
    </caption>
    <thead>
      <tr>
        <th>Código</th>
        <th>Nome</th>
        <th>Opções</th>
      </tr>
    </thead>
    <tbody>
      {{#each majors}}
        <tr>
          <td>{{this.code}}</td>
          <td>{{this.name}}</td>
          <td>
            <a href="/major/update/{{this.id}}" title="Editar"><i class="fas fa-pencil-alt icon"></i></a>
            <a href="/major/read/{{this.id}}" title="Visualizar"><i class="fas fa-eye icon"></i></a>
            <form id="form-remove-{{this.id}}" action="/major/remove/{{this.id}}" method="POST" style="display:inline;">
                <button type="button"
                class="icon-btn"
                title="Excluir"
                onclick="confirmDeletar('{{this.id}}', '{{this.name}}')"
                style="background:none;border:none;padding:0;cursor:pointer;">
                <i class="fas fa-trash-alt icon"></i>
                </button>
            </form>
          </td>
        </tr>
      {{/each}}
    </tbody>
  </table>
</section>

<script src="/js/modal.js"></script>

<!-- Modal de confirmação -->
<div id="modal-confirm" class="modal" style="display:none;">
  <div class="modal-content">
    <span class="close" onclick="fecharModal()">&times;</span>
    <h2>Confirmar deleção</h2>
    <p id="modal-msg">Deseja mesmo apagar este curso?</p>
    <div class="modal-actions">
      <button onclick="fecharModal()">Fechar</button>
      <button id="btn-confirmar" class="btn-confirmar">Sim, apagar curso</button>
    </div>
  </div>
</div>